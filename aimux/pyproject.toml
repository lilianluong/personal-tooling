[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aimux"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.70.0",
]

[project.scripts]
aimux = "aimux.main:main"

[tool.setuptools.packages.find]
where = ["."]
